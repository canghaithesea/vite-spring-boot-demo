spring.application.name=vite-spring-boot-demo

# Zeabur/容器环境通常会注入 PORT；本地不设置时默认 8080
server.port=${PORT:8080}

# 允许跨域来源（逗号分隔）。默认仅允许本地 Vite 开发地址。
app.cors.allowed-origins=${ALLOWED_ORIGINS:http://localhost:5173}
